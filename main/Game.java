package main;

import java.awt.*;

public class Game {
    private GameFrame window;
    public Game(){
        window = new GameFrame();
        window.setPreferredSize(new Dimension(400, 400));
    }
}
